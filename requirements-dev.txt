# Development dependencies — install with: pip install -e ".[dev]"
# or: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=7.0.0
coverage>=7.0.0
black>=23.0.0
isort>=5.0.0
flake8>=6.0.0
mypy>=1.0.0
